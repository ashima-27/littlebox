export class Conn {
    static nodeurl:string="http://localhost:3000";
    //static  because we cannot use class variable hence we use static so that we vcan use variable
    static skey:string="#%%$&^%&";
    static uname:string="#$%#%^&&";
}
